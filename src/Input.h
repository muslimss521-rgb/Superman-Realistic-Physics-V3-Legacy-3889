#pragma once

namespace Input
{
    bool JustPressed(int control);
    bool Pressed(int control);

    bool Forward();
    bool Back();
    bool Left();
    bool Right();
    bool Up();
    bool Down();
    bool Boost();
}
